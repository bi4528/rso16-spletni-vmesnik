import { SeznamMetadaRequest } from './seznam-metada-request.model';

describe('SeznamMetadaRequest', () => {
  it('should create an instance', () => {
    expect(new SeznamMetadaRequest()).toBeTruthy();
  });
});
